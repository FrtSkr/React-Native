/* eslint-disable prettier/prettier */
import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        margin: 5,
        borderRadius: 5,
        borderWidth: 1,
        borderColor: '#DEDEDE',
    },
    inputText:{        
        fontSize: 12,
        color: 'black',
    },
});
